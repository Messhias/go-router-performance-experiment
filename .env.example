SERVER_ADDR=:8080
UPSTREAM_A_URL=http://llama-gemma:8080
UPSTREAM_B_URL=http://llama-qwen:8080
# Allow slow CPU inference and cold model load (override for faster local tests if needed).
UPSTREAM_REQUEST_TIMEOUT=300s
GIN_MODE=release